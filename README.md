## Basic networking program using UDP connection
This is an attempt to produce a program that uses UDP protocol using Enet library.
ensure to run the server first before the client.
```
BASH
>server.exe

in a another separate terminal run.
>client.exe
```
